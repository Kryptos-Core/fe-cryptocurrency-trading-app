/// Default symbol for the platform cash / quote ledger wallet in the UI.
///
/// Keep aligned with backend `PLATFORM_CASH_CURRENCY_SYMBOL` and admin runtime
/// settings; this is the fallback when config is not loaded yet.
const String kDefaultPlatformCashCurrencySymbol = 'USDT';
