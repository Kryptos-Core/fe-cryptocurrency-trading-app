# `lib/shared/`

Cross-feature domain helpers (ví dụ model/picker dùng ở nhiều màn) sẽ đặt tại đây.  
Hiện tại phần lớn UI chọn currency / cặp giao dịch nằm trong `features/markets/presentation/widgets/`.
