# Tests

Widget và unit tests import trực tiếp `package:crypto_trading_app/features/...`.

Thư mục con (`screens/`, `presentation/`, …) là nhóm theo ngữ cảnh; khi thêm test mới, ưu tiên đặt cạnh feature tương ứng dưới `test/features/<feature>/...` nếu tạo cấu trúc mới.
